/*
    Program Descriptor: 3-sum. Given N distinct integers, how many triples sum to exactly zero?

*/
#include <stdio.h>

#define NUM 8

int main()
{
    int array[NUM] = {30, -40, -20, -10, 40, 0, 10, 5};
    int i, j = 0, k = 0;
    
    for(int i = 1; i<NUM; i++)
    {
        if(array[j] > array[i])
        {
            
        }
    }
}